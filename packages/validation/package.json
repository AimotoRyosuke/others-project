{
  "name": "validation",
  "version": "0.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist .tsbuildinfo",
    "type-check": "tsc --noEmit",
    "lint": "eslint src/*.ts",
    "dev": "tsc -b --watch"
  },
  "dependencies": {
    "zod": "latest",
    "types": "file:../types"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
